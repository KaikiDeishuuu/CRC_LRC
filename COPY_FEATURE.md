# 复制功能说明

## 功能概述

现在的复制功能已升级为**复制完整数据**，即点击复制按钮时会同时复制：

- 原始输入数据
- 对应算法的校验值

## 复制格式

### HEX 输入模式

**输入**: `01 02 03 04`  
**CRC16 MODBUS 结果**: `B448`  
**复制内容**: `01 02 03 04 B448`

所有数据以空格分隔的十六进制格式呈现，可直接用于串口通信、网络传输等场景。

### 文本输入模式

**输入**: `Hello`  
**CRC16 MODBUS 结果**: `B448`  
**复制内容**: `48 65 6C 6C 6F B448`

文本会先转换为 HEX 编码（ASCII 值），然后附加校验值，便于理解数据实际传输格式。

## 使用场景

### 1. 串口通信

发送带校验的完整数据包：

```
原始数据: 01 02 03 04
复制后: 01 02 03 04 B448
直接粘贴到串口调试工具即可发送
```

### 2. MODBUS 协议

构造完整的 MODBUS 帧：

```
功能码+数据: 03 00 00 00 0A
复制后: 03 00 00 00 0A C4 09
完整的 MODBUS RTU 帧
```

### 3. 数据验证

快速验证数据完整性：

```
接收数据: 01 02 03 04 B448
使用工具验证: 输入 01 02 03 04 → 校验 B448 ✓
```

## 复制机制

### 三重降级方案

1. **现代 Clipboard API** (优先)

   - 适用于 HTTPS 或 localhost
   - 无感复制，体验最佳

2. **execCommand 降级方案**

   - 兼容旧版浏览器
   - 通过临时 textarea 实现

3. **手动复制对话框**
   - 当上述方法都失败时触发
   - 显示完整数据并提示用户手动复制（Ctrl+C）
   - 点击数据区域自动全选

## 界面提示

结果显示区域会有提示信息：

```
💡 提示：点击"复制"按钮将复制完整数据（输入 + 校验值）
```

## 技术实现

### 核心函数

```typescript
copyToClipboard(checksumValue: string, algorithmName: string, inputData?: string)
```

### 数据转换

- **HEX 模式**: 格式化为空格分隔的 HEX → 追加校验值
- **文本模式**: 转换为 HEX (ASCII) → 追加校验值

### 辅助函数

```typescript
stringToHex(str: string): string
// 将任意字符串转换为空格分隔的 HEX 格式
// 示例: "Hello" → "48 65 6C 6C 6F"
```

## 浏览器兼容性

- ✅ Chrome/Edge (Chromium 内核)
- ✅ Firefox
- ✅ Safari
- ✅ 所有现代浏览器
- ⚠️ HTTP 环境下会降级到 execCommand 或手动复制

## 测试方法

### 快速测试

1. 访问 http://localhost:8080
2. 输入测试数据（如 `01 02 03 04`）
3. 选择算法（如 CRC16 MODBUS）
4. 点击计算
5. 点击"复制"按钮
6. 粘贴到记事本验证

### 验证结果

粘贴后应该看到：

```
01 02 03 04 B448
```

（原始输入 + 校验值）

## 注意事项

1. **空格分隔**: 所有 HEX 数据都以空格分隔，便于阅读和解析
2. **大写格式**: 所有 HEX 字符统一为大写（如 `B448` 而非 `b448`）
3. **完整性**: 确保输入数据和校验值一起复制，避免手动拼接错误
4. **兼容性**: 在 HTTP 环境下可能需要手动复制，建议使用 HTTPS 或 localhost

## 更新日志

- **2024-01-XX**: 升级为复制完整数据功能
  - 支持 HEX 和文本两种输入模式
  - 自动格式化和拼接
  - 三重降级保证可用性
  - 友好的手动复制提示对话框
